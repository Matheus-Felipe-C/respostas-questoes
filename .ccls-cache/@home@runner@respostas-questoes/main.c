#include <stdio.h>

int main() {
  /*  
    Corrija o seguinte programa:
    #include <stdio.h>
    main{void}(
    printf(Existem %d semanas no ano., 56)
    )
*/

  printf("Existem %d semanas no ano.", 56);
  return 0;
}